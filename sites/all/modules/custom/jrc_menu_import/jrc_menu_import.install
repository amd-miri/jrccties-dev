<?php

/**
 * @file
 * Install file.
 */

 /**
  * Implements hook_enable().
  */
function jrc_menu_import_enable() {
  drupal_set_message(t('The JRC menu migration module is enabled. You can now run the import.'), $type = 'status');
}

/**
 * Implements hook_disable().
 */
function jrc_menu_import_disable() {
  drupal_set_message(t('The JRC menu migration module is disabled.'), $type = 'status');
}
